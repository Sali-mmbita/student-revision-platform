SECRET_KEY=your-secret
JWT_SECRET_KEY=your-jwt-secret
# For quick local testing use sqlite:
DATABASE_URL=sqlite:///dev.db

# When ready for MySQL (XAMPP) set these instead & remove DATABASE_URL
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=student_revision
